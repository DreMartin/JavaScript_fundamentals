var firstName = 'Andres';

// a variable can also be declared, but not initialized, for example:

let lastName;

// it can be initialized afterwards, for example:

lastName = 'Martin';

console.log(typeof (firstName));
